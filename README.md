# OpenClaw Workspace Default

Default workspace configuration for OpenClaw instances.

This repository is cloned automatically when an OpenClaw instance starts for the first time.

## Contents

- `SECURITY.md` - Security rules the agent should follow
- `HEARTBEAT.md` - Periodic maintenance tasks

## Usage

This repo is automatically cloned to `/data/workspace` on OpenClaw instances.
